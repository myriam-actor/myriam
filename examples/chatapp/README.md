# chatapp

"small" example app demonstrating the use of local and remote actors.

## usage

set up a hidden service and then run with `cargo run -- <nickname>` where `nickname` corresponds to a unique Tor keystore.

## notes

`!add` might fail the first time. just try again if so.
